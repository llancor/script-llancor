import { db,decryptSecret } from './lib.js';
const encodePath=(path:string)=>path.split('/').filter(Boolean).map(encodeURIComponent).join('/');
async function settings(){const c=await db.configuracion.findUnique({where:{id:1}});if(!c?.nextcloud_enabled||!c.nextcloud_url||!c.nextcloud_user||!c.nextcloud_password)throw new Error('Nextcloud no está configurado');const root=c.nextcloud_url.replace(/\/$/,'')+`/remote.php/dav/files/${encodeURIComponent(c.nextcloud_user)}`,auth='Basic '+Buffer.from(`${c.nextcloud_user}:${decryptSecret(c.nextcloud_password)}`).toString('base64');return{c,root,headers:{Authorization:auth}}}
export async function testNextcloud(){const{root,headers}=await settings();const r=await fetch(root+'/',{method:'PROPFIND',headers:{...headers,Depth:'0'}});if(!r.ok&&r.status!==207)throw new Error(`Nextcloud respondió ${r.status}`)}
export async function uploadNextcloud(worker:{documento:string;nombre:string},file:{dataUrl:string;name:string}){const{c,root,headers}=await settings(),base=String(c.nextcloud_folder||'BastControl').replace(/^\/+|\/+$/g,''),person=`${worker.documento}-${worker.nombre}`.replace(/[^\p{L}\p{N}._-]+/gu,'_'),dirs=[base,`${base}/${person}`];for(const dir of dirs){const r=await fetch(`${root}/${encodePath(dir)}`,{method:'MKCOL',headers});if(!r.ok&&r.status!==405)throw new Error(`No se pudo crear la carpeta Nextcloud (${r.status})`)}const match=file.dataUrl.match(/^data:([^;]+);base64,(.+)$/);if(!match)throw new Error('Archivo adjunto inválido');const path=`${base}/${person}/${Date.now()}-${file.name.replace(/[^\p{L}\p{N}._-]+/gu,'_')}`,r=await fetch(`${root}/${encodePath(path)}`,{method:'PUT',headers:{...headers,'Content-Type':match[1]},body:Buffer.from(match[2],'base64')});if(!r.ok)throw new Error(`No se pudo subir a Nextcloud (${r.status})`);return path}
